This file has no file extension at all.

It tests how the Gemini server handles files without extensions.
The server should either:
- Use content detection to determine MIME type
- Fall back to a default type like text/plain or application/octet-stream
- Handle it gracefully without errors

Files without extensions are common in Unix-like systems and
the server should handle them properly.

Content: Plain text format
MIME type detection test for extensionless files.